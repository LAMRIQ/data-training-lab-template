name: "data_training_lab"
version: "1.0.0"
config-version: 2

profile: "data_training_lab"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  data_training_lab:
    staging:
      +materialized: view
    marts:
      +materialized: table
